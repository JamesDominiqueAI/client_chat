[project]
name = "customer-report-agent"
version = "0.1.0"
description = "MCP-architected customer support reporting chatbot."
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
  "backend/api",
]
