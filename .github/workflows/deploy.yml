name: Deploy Client Chat

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy"
        required: true
        default: development
        type: choice
        options:
          - development
          - production
      git_ref:
        description: "Branch, tag, or commit SHA to deploy."
        required: false
        default: main

permissions:
  id-token: write
  contents: read

concurrency:
  group: client-chat-deploy-${{ github.event.inputs.environment || 'development' }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to ${{ github.event.inputs.environment || 'development' }}
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'development' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.git_ref || github.sha }}

      - name: Resolve deployment configuration
        id: deploy_config
        env:
          TARGET_ENVIRONMENT: ${{ github.event.inputs.environment || 'development' }}
          ROLE_ARN_ENV: ${{ secrets.AWS_ROLE_ARN }}
          ROLE_ARN_DEV_LEGACY: ${{ secrets.AWS_ROLE_ARN_DEV }}
          AWS_REGION_PRIMARY: ${{ secrets.DEFAULT_AWS_REGION }}
          PROJECT_NAME_ENV: ${{ secrets.PROJECT_NAME }}
          PROJECT_NAME_DEV_LEGACY: ${{ secrets.PROJECT_NAME_DEV }}
          COMPLAINTS_TABLE_ENV: ${{ secrets.COMPLAINTS_TABLE_NAME }}
          COMPLAINTS_TABLE_DEV_LEGACY: ${{ secrets.COMPLAINTS_TABLE_NAME_DEV }}
          AUDIT_TABLE_ENV: ${{ secrets.AUDIT_TABLE_NAME }}
          AUDIT_TABLE_DEV_LEGACY: ${{ secrets.AUDIT_TABLE_NAME_DEV }}
          MANAGER_PASSWORD_ENV: ${{ secrets.MANAGER_PASSWORD }}
          MANAGER_PASSWORD_DEV_LEGACY: ${{ secrets.MANAGER_PASSWORD_DEV }}
          MANAGER_AUTH_SECRET_ENV: ${{ secrets.MANAGER_AUTH_SECRET }}
          MANAGER_AUTH_SECRET_DEV_LEGACY: ${{ secrets.MANAGER_AUTH_SECRET_DEV }}
          SLACK_WEBHOOK_ENV: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_DEV_LEGACY: ${{ secrets.SLACK_WEBHOOK_URL_DEV }}
          ALARM_EMAIL_ENV: ${{ secrets.ALARM_EMAIL }}
          ALARM_EMAIL_DEV_LEGACY: ${{ secrets.ALARM_EMAIL_DEV }}
          TF_STATE_BUCKET_VALUE: ${{ secrets.TERRAFORM_STATE_BUCKET }}
          TF_LOCK_TABLE_VALUE: ${{ secrets.TERRAFORM_LOCK_TABLE }}
        run: |
          aws_region="${AWS_REGION_PRIMARY:-us-east-1}"

          if [[ "${TARGET_ENVIRONMENT}" == "development" ]]; then
            role_arn="${ROLE_ARN_ENV:-${ROLE_ARN_DEV_LEGACY:-}}"
            project_name="${PROJECT_NAME_ENV:-${PROJECT_NAME_DEV_LEGACY:-client-chat-dev}}"
            complaints_table="${COMPLAINTS_TABLE_ENV:-${COMPLAINTS_TABLE_DEV_LEGACY:-client-chat-dev-complaints}}"
            audit_table="${AUDIT_TABLE_ENV:-${AUDIT_TABLE_DEV_LEGACY:-client-chat-dev-audit}}"
            manager_password="${MANAGER_PASSWORD_ENV:-${MANAGER_PASSWORD_DEV_LEGACY:-}}"
            manager_auth_secret="${MANAGER_AUTH_SECRET_ENV:-${MANAGER_AUTH_SECRET_DEV_LEGACY:-}}"
            slack_webhook="${SLACK_WEBHOOK_ENV:-${SLACK_WEBHOOK_DEV_LEGACY:-}}"
            alarm_email="${ALARM_EMAIL_ENV:-${ALARM_EMAIL_DEV_LEGACY:-}}"
            session_name="client-chat-dev-github-actions"
            missing_secret_hint="development environment"
          else
            role_arn="${ROLE_ARN_ENV:-}"
            project_name="${PROJECT_NAME_ENV:-client-chat}"
            complaints_table="${COMPLAINTS_TABLE_ENV:-client-chat-complaints}"
            audit_table="${AUDIT_TABLE_ENV:-client-chat-audit}"
            manager_password="${MANAGER_PASSWORD_ENV:-}"
            manager_auth_secret="${MANAGER_AUTH_SECRET_ENV:-}"
            slack_webhook="${SLACK_WEBHOOK_ENV:-}"
            alarm_email="${ALARM_EMAIL_ENV:-}"
            session_name="client-chat-prod-github-actions"
            missing_secret_hint="production environment"
          fi

          if [[ -z "$role_arn" ]]; then
            echo "::error::Missing AWS role ARN for ${TARGET_ENVIRONMENT}. Set AWS_ROLE_ARN on that GitHub environment."
            exit 1
          fi

          if [[ -z "${TF_STATE_BUCKET_VALUE:-}" ]]; then
            echo "::error::Missing TERRAFORM_STATE_BUCKET secret."
            exit 1
          fi

          if [[ -z "${TF_LOCK_TABLE_VALUE:-}" ]]; then
            echo "::error::Missing TERRAFORM_LOCK_TABLE secret."
            exit 1
          fi

          if [[ -z "$manager_password" ]]; then
            echo "::error::Missing manager password secret for ${missing_secret_hint}."
            exit 1
          fi

          if [[ -z "$manager_auth_secret" ]]; then
            echo "::error::Missing manager auth secret for ${missing_secret_hint}."
            exit 1
          fi

          {
            echo "target_environment=$TARGET_ENVIRONMENT"
            echo "role_arn=$role_arn"
            echo "aws_region=$aws_region"
            echo "project_name=$project_name"
            echo "complaints_table=$complaints_table"
            echo "audit_table=$audit_table"
            echo "manager_password=$manager_password"
            echo "manager_auth_secret=$manager_auth_secret"
            echo "slack_webhook=$slack_webhook"
            echo "alarm_email=$alarm_email"
            echo "tf_state_bucket=${TF_STATE_BUCKET_VALUE}"
            echo "tf_lock_table=${TF_LOCK_TABLE_VALUE}"
            echo "session_name=$session_name"
          } >> "$GITHUB_OUTPUT"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.deploy_config.outputs.role_arn }}
          role-session-name: ${{ steps.deploy_config.outputs.session_name }}
          aws-region: ${{ steps.deploy_config.outputs.aws_region }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run deployment script
        env:
          DEFAULT_AWS_REGION: ${{ steps.deploy_config.outputs.aws_region }}
          PROJECT_NAME: ${{ steps.deploy_config.outputs.project_name }}
          COMPLAINTS_TABLE_NAME: ${{ steps.deploy_config.outputs.complaints_table }}
          AUDIT_TABLE_NAME: ${{ steps.deploy_config.outputs.audit_table }}
          MANAGER_PASSWORD: ${{ steps.deploy_config.outputs.manager_password }}
          MANAGER_AUTH_SECRET: ${{ steps.deploy_config.outputs.manager_auth_secret }}
          SLACK_WEBHOOK_URL: ${{ steps.deploy_config.outputs.slack_webhook }}
          ALARM_EMAIL: ${{ steps.deploy_config.outputs.alarm_email }}
          DEPLOY_ENVIRONMENT: ${{ steps.deploy_config.outputs.target_environment }}
          TERRAFORM_STATE_BUCKET: ${{ steps.deploy_config.outputs.tf_state_bucket }}
          TERRAFORM_LOCK_TABLE: ${{ steps.deploy_config.outputs.tf_lock_table }}
        run: bash scripts/deploy_aws.sh

      - name: Get deployment outputs
        id: deploy_outputs
        env:
          AWS_REGION: ${{ steps.deploy_config.outputs.aws_region }}
          DEPLOY_ENVIRONMENT: ${{ steps.deploy_config.outputs.target_environment }}
          TERRAFORM_STATE_BUCKET: ${{ steps.deploy_config.outputs.tf_state_bucket }}
          TERRAFORM_LOCK_TABLE: ${{ steps.deploy_config.outputs.tf_lock_table }}
        run: |
          terraform -chdir=terraform/4_frontend init -input=false -reconfigure \
            -backend-config="bucket=${TERRAFORM_STATE_BUCKET}" \
            -backend-config="key=${DEPLOY_ENVIRONMENT}/terraform/4_frontend/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TERRAFORM_LOCK_TABLE}" \
            -backend-config="encrypt=true"
          echo "api_base_url=$(terraform -chdir=terraform/4_frontend output -raw api_base_url)" >> "$GITHUB_OUTPUT"
          echo "cloudfront_domain_name=$(terraform -chdir=terraform/4_frontend output -raw cloudfront_domain_name)" >> "$GITHUB_OUTPUT"
          echo "frontend_bucket_name=$(terraform -chdir=terraform/4_frontend output -raw frontend_bucket_name)" >> "$GITHUB_OUTPUT"

      - name: Smoke test API
        run: curl --fail --silent --show-error "${{ steps.deploy_outputs.outputs.api_base_url }}/health"

      - name: Deployment Summary
        run: |
          echo "Deployment complete."
          echo "Frontend: https://${{ steps.deploy_outputs.outputs.cloudfront_domain_name }}"
          echo "API:      ${{ steps.deploy_outputs.outputs.api_base_url }}"
          echo "Bucket:   ${{ steps.deploy_outputs.outputs.frontend_bucket_name }}"
