# Agents

Reserved phase for future async-agent infrastructure such as queues, worker Lambdas, and scheduled operations jobs.

The current app runs MCP flows through the API synchronously, so this phase is intentionally minimal.
